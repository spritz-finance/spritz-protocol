# =============================================================================
# Spritz Protocol Environment Variables
# =============================================================================
# These override values in config.json. Leave commented to use config defaults.
# Priority: environment variable > .env > config.json

# Alchemy RPC Key (required)
RPC_KEY=your_alchemy_key_here

# Etherscan API Key (for contract verification)
# Works across all Etherscan-compatible explorers (Basescan, Arbiscan, etc.)
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# Safe API Key (required for Safe transaction proposals)
# Get one from https://safe.global/
SAFE_API_KEY=your_safe_api_key_here

# =============================================================================
# Deployer Overrides (optional - defaults from config.json)
# =============================================================================
# Override deployer address and 1Password key reference.
# IMPORTANT: If you override DEPLOYER_ADDRESS, you must also override salts
# because CreateX validates that msg.sender matches the first 20 bytes of the salt.

# DEPLOYER_ADDRESS=0x...
# DEPLOYER_KEY_REF=op://Personal/test-deployer/pk

# Per-contract salt overrides (format: <CONTRACT_NAME>_SALT)
# These override the salts in config.json. The salt must start with your deployer address.
# Generate new salts with: bun salt <ContractName>

# SPRITZPAYCORE_SALT=0x...
# SPRITZROUTER_SALT=0x...

# =============================================================================
# Safe Signers (used by bun safe)
# =============================================================================
# Format: op://Vault/Item/Field

SIGNER_1_NAME=deployer
SIGNER_1_ADDRESS=0xbadfaceB351045374d7fd1d3915e62501BA9916C
SIGNER_1_KEY_REF=op://Personal/spritz-deployer/pk

SIGNER_2_NAME=personal
SIGNER_2_ADDRESS=0x...
SIGNER_2_KEY_REF=op://Personal/personal-signer/pk

# Optional: Add more signers (up to 10)
# SIGNER_3_NAME=backup
# SIGNER_3_ADDRESS=0x...
# SIGNER_3_KEY_REF=op://Personal/backup-signer/pk
