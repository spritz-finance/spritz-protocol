# Compiler files
cache/
out/

# Dependencies (restored via `forge install` or `git submodule update --init --recursive`)
lib/

# Forge broadcast logs (we track deployments in deployments/*/metadata.json)
broadcast/


# Dotenv file
.env

.DS_Store
